# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.env
.venv/

# Node
node_modules/
dist/
build/
coverage/

# Models (Keep small artifacts, but ignore huge ones if they exist, though user wants under 250MB so 10MB is fine)
# *.pkl (Not ignoring, we need them for now or user can generate them)

# Data
# data/job_market_analytics_dataset.csv (Keep it, it's small ~47KB)
# data/candidates.csv (Keep it, ~5MB)

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.eslintcache
frontend/coverage/

# IDE
.vscode/
.idea/
.gemini/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
